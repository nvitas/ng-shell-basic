'use strict';

app.constant("globalResolve", {
	resolveBeforeEachRoute : {
		//'Load Local Stored Data' : ['localStorage', function (localStorage) {
		//	logger.log("Loading local data...");
		//	return localStorage.load();
		//}]
	}
});
